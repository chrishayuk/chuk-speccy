# Rust build output (any workspace, incl. the zxspec_py sub-workspace)
target/
Cargo.lock
**/*.rs.bk

# Python (zxspec_py wheels, chuk-mcp-spectrum)
__pycache__/
*.py[cod]
*.egg-info/
*.whl
*.so
.venv/
.pytest_cache/

.DS_Store
/testroms

# generated dialect-compiler / asset output (speccy-compile / -run / -asset)
/*.tap
/*.sym.toml
/*.scr
/*.gif
